// js/config.js

// Altere para a URL base do seu servidor Java Spring Boot / Quarkus / Jakarta EE
const API_CONFIG = {
    BASE_URL: 'http://localhost:8080',
    ENDPOINTS: {
        LOGIN: '/api/auth/login',
        PERFIL: '/perfil'
    }
};
